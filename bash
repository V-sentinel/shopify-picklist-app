# Install Shopify CLI
npm install -g @shopify/cli @shopify/theme

# Create a new Shopify app
shopify app init picklist-app --template node
cd picklist-app

# Copy your existing backend code into the app
